import React from 'react';
import NavBar from './NavBar';

const Form = () => {
    return (
        <div>
            <NavBar />
            <h1>Form</h1>
        </div>
    );
};

export default Form;