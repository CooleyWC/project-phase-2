import { createTheme } from "@mui/material/styles";


const theme = createTheme({
    palette: {
        primary: {
            main: '#007bff'
        },
        secondary: {
            main: '#6c5ce7'
        },
        background: {
            default: '#333333'
        }
    }
})

export default theme;